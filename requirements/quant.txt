# Vol-engine only. Adds arch (GARCH(1,1)) on top of the IB stack.
# Kept separate because arch pulls ~40 MB of stats deps that the other
# services never import.

-r ib.txt

arch>=6.0
# Step 1 regime gating : reads feature_history / events / vrp_table_default
# from Postgres each cycle (cf. docs/vol_trading_pca/specs/STEP1_REGIME_GATING.md §7).
SQLAlchemy>=2.0,<2.1
asyncpg>=0.29,<0.30
# Step 1 §3 zone 2 — GMM 3-component over (vol_level, vol_of_vol, term_slope)
# to produce P(regime=calm/stressed/pre_event). Replaces the threshold heuristic
# once feature_history has accumulated enough obs (cf. core/vol/gmm_regime.py).
scikit-learn>=1.4,<1.7
